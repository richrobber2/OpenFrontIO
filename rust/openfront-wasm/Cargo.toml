[package]
name = "openfront-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
openfront-core = { path = "../openfront-core" }

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
